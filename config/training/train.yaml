# Exact timestamped directory created by prepare_space_training.py.
splits_dir: ./logs/prep/yongning-na_01/2026-08-11-13-37-22

model_name: bilstm-boundary
embedding_dim: 128
hidden_size: 256
num_layers: 2
dropout: 0.2

train_seed: 42
batch_size: 64
eval_batch_size: 128
epochs: 50
learning_rate: 0.001
weight_decay: 0.01
max_grad_norm: 1.0
patience: 7
device: auto

# Optional. By default this is derived from the manifests as <dataset-root>/processed/spaces.
# out_dir: ~/projects/my-projects/asr-models/language-downloads/yongning-na_01/processed/spaces
